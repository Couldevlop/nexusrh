# CLAUDE.md — Dépôt OPENLAB / NexusRH

> **Projet actif = `nexusrh_ci/`** (SIRH SaaS multi-tenant, Côte d'Ivoire).
> Tout le travail se fait dans `nexusrh_ci/` sauf demande contraire explicite.
> Le contexte maître complet est dans **`nexusrh_ci/CLAUDE.md`** (chargé automatiquement
> dès qu'on touche un fichier sous `nexusrh_ci/`), avec ses specs détaillées dans
> `nexusrh_ci/docs/reference/`.

## Versionnement

- Seul **`nexusrh_ci/`** est versionné (git). La racine `nexusrh/` n'est **pas** versionnée.
- Les nouveaux fichiers sous `nexusrh_ci/` sont ignorés par le `.gitignore` racine → **`git add -f`** obligatoire.
- Branche de travail : **`develop`**. Pipeline de déploiement : push `main` → namespace `nexusrh-ci`.

## Template France générique (archivé)

La version France générique de ce CLAUDE.md (TechCorp/SYNTEC, paie France) n'est plus
le projet actif. Elle est conservée pour référence dans
**`docs/reference/CLAUDE-france-template.md`** au cas où le template générique serait repris.

---

_Pour toute tâche : ouvrir `nexusrh_ci/CLAUDE.md`._
