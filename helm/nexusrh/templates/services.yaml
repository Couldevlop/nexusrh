---
# Service API
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexusrh.fullname" . }}-api
  labels:
    {{- include "nexusrh.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexusrh.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
---
# Service Web
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexusrh.fullname" . }}-web
  labels:
    {{- include "nexusrh.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexusrh.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
